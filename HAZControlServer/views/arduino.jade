// arduino.js
//=======================================================================

block content
  h1 #{title}
  p
  a(href='/arduino/new', class='btn primary') Incluir Nuevo Arduino
- if(docs.length)
  table
    tr
      th Arduino
        each arduino in docs
          tr
            td #{arduino.nombre}
            td #{arduino.ip}
            td #{arduino.mac}
            td #{arduino.netmask}
            td #{arduino.gateway}
            td #{arduino.dns}
            td #{arduino.uso}
- else
  p No tienes ningun Arduino
